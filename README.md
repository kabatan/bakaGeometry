# GeoSolver Guardian Instruction Pack v2.2

This pack contains the Base Spec, Plan, reviewer prompts, schemas, source map, patch notes, and consistency audit for implementing the R-GDTPK-Q / ACCTP-Q solver core under Guardian.

Use the files under:

```text
docs/ai/changes/RGDTPK-Q-v4-core/
```

Do not implement from earlier v2 or v2.1 drafts. v2.2 adds final consistency hardening `RGQ-057` through `RGQ-064`.

Recommended read order: `BASE_SPEC.md`, `PLAN.md`, `REVIEWER_PROMPTS.md`, `REVIEW_ARCHIVE_SCHEMA.md`, `CONSISTENCY_AUDIT.md`, `SOURCE_MAP.md`.
