# geosolver-core

`geosolver-core` is a Rust library crate for the CW-ARC-DTP-Q certified target candidate-cover solver over rational polynomial systems.

The active implementation authority is `docs/ai/changes/cw-arc-dtp-q/BASE_SPEC.md`.
