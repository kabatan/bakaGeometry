# P0 Implementation Authority

Status: user implementation authority evidence.

User approval source: current Codex conversation.  
Date: 2026-07-07.  
Scope approved: amended `RGDTPK-Q-v4-finite-candidate-cover` Base Spec and Plan.

Exact user approval text:

```text
では実装を開始し、仕様書通りのアルゴリズムを計画に忠実に完成させてください
```

Interpretation:

- The user approved starting implementation after the finite candidate-cover scope amendment.
- The approval is scoped to the amended Base Spec and Plan, not full-v4 exact-image conformance.
- This approval does not grant deletion authority outside the admitted ReadSet, does not verify any
  R-ID, and does not authorize final readiness claims.
