P12G-d implemented in `finalize_candidate_cover_result`. Regression
`p12g_candidate_cover_with_no_real_roots_is_empty_cover_not_hard_case` verifies support `T^2 + 1`
returns `CertifiedCandidateCover` with empty roots/candidates and emits the
`EmptyRealCandidateCover` diagnostic.
